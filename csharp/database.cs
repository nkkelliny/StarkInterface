using System;
using System.Data;
using System.Data.SqlClient;
using System.Data.Oleb;


class databasepullup
{
	static void Main()
	{
		String connectionString = "DataSource= (MySql_connection); 
		integratedSecurity="true";
	}
		StringQuery = " "SELECT ProductID"
		
	using (var client = new HTTPCLient())
		client.BaseAddress = new Uri ("http://watever.com");
		client.DefaultRequestHeaders.Accept.Clear();
		client.DEfaultREquestHeaders.Accept.Add("New Database Type");
	{
}
